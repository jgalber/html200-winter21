// Exercise 2 complete
const arr = ['a', 'group', 'of', 'gypsies', 'are', 'dancing', 'flamenco'];
// Display only the third word
  console.log(arr[2]); 
// Join the string in one sentence
let str = arr.join(' ');
  console.log(str);
// Add the period at the end of the sentence
let str2 = str.concat('.');
  console.log(str2);